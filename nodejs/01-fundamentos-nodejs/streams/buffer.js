
// Buffer => representação de dados na memória (binário)
//    -> maneira mais otimizada de escrever e ler da memória, utilizada pelas streams do NodeJS

const buf = Buffer.from('test')
        
console.log(buf)